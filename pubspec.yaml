name: estoque_salao_de_cabelo
description: Sistema de gestão de estoque para salão de cabelo.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Versão do SDK atualizada para compatibilidade

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.4.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0 # Versão do lints atualizada
  hive_generator: ^2.0.1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true